// STUB -- unused. background/identity/derive.ts, storage.ts, and
// handler.ts (M5) are imported directly by relative path elsewhere; nothing
// imports through this index. Exists so the directory shape is stable from
// Phase 1 onward -- Phase 2 adds files here, not a restructure.
export {};
