// STUB -- Phase 2 (Local Identity Vault) implements this module.
// No message types are routed here yet; router/registry.ts has no
// `identity` rows. Exists so the directory shape is stable from Phase 1
// onward -- Phase 2 adds files here, not a restructure.
export {};
