// STUB -- Phase 3 (Identity Firewall) implements this module.
// No message types are routed here yet; router/registry.ts has no
// `firewall` rows. Exists so the directory shape is stable from Phase 1
// onward -- Phase 3 adds files here, not a restructure.
export {};
