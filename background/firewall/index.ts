// STUB -- unused. classifier.ts, responseAvailability.ts,
// syntheticGenerator.ts, responseGenerator.ts, handler.ts (Phase 3), and
// loginDetector.ts (Phase 5 M3) are all imported directly by relative
// path elsewhere; nothing imports through this index. Exists so the
// directory shape is stable from Phase 1 onward -- new phases add files
// here, not a restructure.
export {};
